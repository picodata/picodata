## feat/sql

- Support `WAIT APPLIED GLOBALLY/LOCALLY` syntax for plugin operations
  (`CREATE PLUGIN`, `ALTER PLUGIN`, `DROP PLUGIN`).
- Plugin operations have `WAIT APPLIED GLOBALLY` semantic by default now.
