status: "in progress" decision-makers: @funbringer, @kostja, @andrewio

--------------------------------

# Возможность ограничения количества сетевых пересылок во время исполнения запроса

## Мотивация

В настоящий момент при получении SQL запроса Пикодата вычисляет множество
бакетов, на которых должен выполниться запрос, и рассылает его соответствующим
узлам. Если все требуемые бакеты содержатся на одном узле, запрос можно
выполнить на этом узле без сетевых пересылок. Этот факт широко используется в
"умных" драйверах для увеличения производительности. Драйвер заранее вычисляет
множество бакетов для запроса и отправляет его на один из узлов
репликасета-владельца. Подробнее с идеей можно ознакомиться в
[**этом**](https://git.picodata.io/core/picodata/-/blob/984b6a76a3b9a75c6ee4803ecaa99b3e879c4338/doc/adr/2025-09-12-pgproto-query-metadata.md)
ADR.

В рамках текущего АДР предлагается дать возможность пользователям ограничивать
количество пересылок по сети во время исполнения DQL и DML запросов. Так, если
для исполнения запроса требуется больше пересылок, чем разрешено, то
пользователь получит ошибку на этапе исполнения запроса.

## Решение

### Опция `forward`

Предлагается добавить опцию **forward** (см. задачу
[#2722](https://git.picodata.io/core/picodata/-/issues/2722)), которая может
иметь три различных значения:

1. **on** - бакеты, на которых будет исполняться запрос, принадлежат разным
   узлам; в таком случае запрос будет выполнен через scatter-gather по лидерам
   затронутых репликасетов.

2. **ro_to_rw** - все бакеты запроса должны принадлежать **одному узлу**, но при
   исполнении _допустима пересылка_ с координатора запроса на лидера
   соответствующего бакетам репликасета. **Иначе кидаем ошибку.**

3. **off** - все бакеты должны принадлежать **одному узлу**, а клиент обязан сам
   отправить запрос на лидера соответствующего бакету репликасета. **Иначе
   кидаем ошибку.**

Иными словами, мы имеем нарастающую степень локальности:
1. локальности нет ни в каком смысле;
2. по крайней мере, все бакеты запроса принадлежат одному узлу;
3. истинная локальность (клиент отправил запрос на узел, хранящий бакеты).

Значение опции можно будет указывать в части `OPTION` SQL запроса и в connection
string по аналогии с уже существующими `sql_vdbe_max_row`, `sql_vdbe_opcode_max`
и `read_replica`. Например:

```sql
SELECT * FROM t WHERE a = 1 OPTION (FORWARD = OFF);
```

#### Взаимодействие опции `forward` с опцией `read_preference`

В настоящий момент Пикодата позволяет для DQL запросов указывать опцию
`read_preference`, с помощью которой можно выбрать на каких узлах будет
исполняться запрос. Подробнее с опцией можно ознакомиться в
[**документации**](https://docs.picodata.io/picodata/devel/reference/sql/dql/#params).

Ниже приведена таблица всех возможных комбинаций двух опций. Галочкой отмечены
разрешенные комбинации. В остальных случаях пользователь получит ошибку с
объяснением, почему комбинация невалидна.


| `read_preference` / `forward` |  `on`  | `ro_to_rw` | `off` | 
|:-----------------------------:|:------:|:----------:|:-----:|
|            `any`              |   ☑    |     ☑      |   ✖   |
|           `leader`            |   ☑    |     ☑      |   ☑   |
|           `replica`           |   ☑    |     ☑      |   ✖   |

В случае `read_preference = any | replica` запрос может быть выполнен на
реплике, что противоречит `forward = off`. При указании такой комбинации
пользователь получит ошибку. Таким образом, опция `forward` имеет приоритет над
`read_preference`.

### Принцип исполнения запросов с моушенами (motion)

Рассмотрим как исполняются SQL запросы в Пикодате. Будем использовать таблицы

```sql
CREATE TABLE a (id INT PRIMARY KEY, val INT);
CREATE TABLE b (id INT PRIMARY KEY, val INT);
CREATE TABLE c (id INT PRIMARY KEY, val INT);
```

и запрос

```sql
INSERT INTO a
    SELECT * FROM b
    WHERE b.id IN (SELECT id FROM c ORDER BY id LIMIT 10);
```

Рассмотрим его `explain`:

```sql
 insert "a" on conflict: fail
     -- motion3
     motion [policy: local segment([ref("id")]), program: ReshardIfNeeded]
         projection ("b"."id"::int -> "id", "b"."val"::int -> "val")
             selection "b"."id"::int in ROW($0)
                 scan "b"
 subquery $0:
 -- motion2
 motion [policy: segment([ref("id")]), program: ReshardIfNeeded]
     scan
         limit 10
             projection ("id"::int -> "id")
                 order by ("id"::int)
                     -- motion1
                     motion [policy: full, program: ReshardIfNeeded]
                         limit 10
                             projection ("id"::int -> "id")
                                 order by ("id"::int)
                                     scan
                                         projection ("c"."id"::int -> "id")
                                             scan "c"
 execution options:
     sql_vdbe_opcode_max = 45000
     sql_motion_row_max = 5000
 buckets = [1-3000]
```

Анализируя вывод выше, можно заметить, что каждый `motion` является корнем
некоторого поддерева в плане. Здесь и далее мы будем использовать слова
`motion`, моушен и поддерево взаимозаменяемо.

Данный запрос будет исполняться в четыре стадии. Моушены будут выполнены в
порядке `motion1`, `motion2`, `motion3`. **Для каждого моушена** Пикодата
сначала рассчитает множество бакетов, а потом проверит, удовлетворяет ли оно
указанной опции `forward`. Если нет, то клиент получит ошибку и запрос прекратит
исполнение; иначе Пикодата выполнит `motion` узел и перейдет к следующему.

Важно отметить, что хоть опция и проверяется во время исполнения запроса, т.е.
динамически, она **не может** привести к ситуации, когда часть DML выполнилась,
а часть нет. **Т.е. невозможна ситуация "разорванного" DML**.

### Особенности динамического анализа

Проверка выполнимости опции `forward` осуществляется во время исполнения
запроса. Такой подход приводит к отсутствию детерминизма, т.е. если запрос в
данный момент выполнился с указанной опцией `forward = off`, то **нет никакой
гарантии**, что он будет успешно выполнен снова после DML запросов над таблицами
`b` и `c`.

Рассмотрим описанное выше на примере. Пусть есть таблицы

```sql
CREATE TABLE a (id INT PRIMARY KEY, val INT);
CREATE TABLE b (id INT PRIMARY KEY, val INT);
CREATE TABLE c (id INT PRIMARY KEY, val INT);
```

Данные, лежащие в таблицах `b` и `c`:

```sql
admin=> SELECT bucket_id, * FROM b;
 bucket_id | id | val
-----------+----+-----
      1934 |  1 |   1
      1410 |  2 |   2
(2 rows)

admin=> SELECT bucket_id, * FROM c;
 bucket_id | id | val
-----------+----+-----
      1410 |  2 |   2
(1 row)
```

Рассматриваемый запрос:

```sql
INSERT INTO a
    SELECT * FROM b
    WHERE b.id IN (SELECT id FROM c ORDER BY id LIMIT 10);
```

Конфигурация кластера, используемая в примерах:
- replicaset1 : buckets [1-1500]
- replicaset2 : buckets [1501-3000]

Репликасет `replicaset1` содержит бакеты от 1 до 1500, а репликасет
`replicaset2` содержит бакеты от 1501 до 3000. Будем считать, что для запроса
ниже узлом-координатором будет мастер репликасета `replicaset1`.

Выполним запрос с опцией `forward = off`:

```sql
-- исполняем на узле, содержащем бакет
INSERT INTO a
    SELECT * FROM b
    WHERE b.id IN (SELECT id FROM c ORDER BY id LIMIT 10);
```

Он успешно выполнится, т.к. результатом подзапроса будет значение `2`,
соответствующее значению бакета `1410`, который принадлежит текущему узлу.

Вставим еще одну строку в таблицу `c`:

```sql
INSERT INTO c VALUES (1, 1);
```

Теперь выполнение запроса с опцией `forward = off` приведет к ошибке 

```
ERROR:  sbroad: invalid option: cannot satisfy "forward = off" option
```

т.к. результатом подзапроса будут значения `1` и `2`, соответствующие бакетaм
`1934` и `1410`, лежащим на разных узлах.

### Статический анализ допустимых опций для `forward`

Вместе с этим предлагается дать пользователю возможность узнать, с какими
значениями опции forward запрос гарантированно сможет выполниться. Эта
функциональность будет добавлена в вывод explain вне зависимости от указания
опции в запросе. Анализ допустимых опций будет производиться статически , т.е.
без выполнения самого запроса. Важно отметить, что анализ будет выполнен на
этапе **`Bind`** после подстановки параметров.

Рассмотрим конкретные примеры. Будем использовать следующие
таблицы:

```sql
CREATE TABLE t (a INT PRIMARY KEY, b INT);
CREATE TABLE g (a INT PRIMARY KEY, b INT) DISTRIBUTED GLOBALLY;
```

Конфигурация кластера, используемая в примерах:
- replicaset1 : buckets [1-1500]
- replicaset2 : buckets [1501-3000]

Репликасет `replicaset1` содержит бакеты от 1 до 1500, а репликасет
`replicaset2` содержит бакеты от 1501 до 3000. Будем считать, что для всех
запросов узлом-координатором будет мастер репликасета `replicaset1`.

Здесь и далее мы будем скрывать вывод `explain`, не относящийся к информации об
опции `forward` и бакетах запроса. Позднее это также станет доступно через
указание фасета `buckets` в команде `explain`.

#### Вариант `forward = off`

```sql
-- запускаем на узле, содержащим данный бакет
SELECT * FROM t WHERE a = 5;
```

```sql
-- explain (buckets)
forward analysis (on > ro_to_rw > off):
    forward = off
buckets = [219]
```

В примере выше максимально допустимой опцией `forward` является `off`, т.к.
Пикодата статически вычислила бакеты запроса и определила, что они принадлежат
текущему узлу.

#### Вариант `forward = ro_to_rw`

```sql
-- запускаем на узле, который не содержит заданный бакет
DELETE FROM t WHERE a = 1;
```

```sql
-- explain (buckets)
forward analysis (on > ro_to_rw > off):
    forward = ro_to_rw
buckets = [1934]
```

Для запроса выше Пикодата статически определила бакеты, но максимально
допустимой опцией будет `ro_to_rw`, т.к. бакеты отсутствуют на текущем узле.

#### Вариант `forward = on`

```sql
-- невозможно статически вычислить бакеты
INSERT INTO t SELECT * FROM g;
```

```sql
-- explain (buckets)
forward analysis (on > ro_to_rw > off):
    forward = on
buckets = unknown
```

Для запроса выше Пикодата не может статически рассчитать бакеты, поэтому
единственной доступной опцией `forward` будет являться только `on`.

С другой стороны, возможна ситуация, когда бакеты, вычисленные из строк таблицы
`g`, будут находиться на одном узле. В таком случае запрос может быть выполнен с
более строгими ограничениями.

Иными словами, вывод допустимых опций в explain является best-effort попыткой
указать пользователю, с какими опциями запрос гарантированно может быть
выполнен.

### Примеры использования опции `forward`

В данном разделе рассматриваются применения опции `forward` во время работы с
Пикодатой. В каждом подразделе будем подразумевать, что запросы используют
таблицу t:

```sql
CREATE TABLE t (a INT PRIMARY KEY, b INT);
```

#### `INSERT` запросы

Рассмотрим запрос:
```sql
-- здесь используется расширенный синтаксис psql для параметров
INSERT INTO t VALUES ($1, $2), ($3, $4) \bind 1 2 3 4 \g
```

Пусть его `explain` выглядит так:
```sql
forward analysis (on > ro_to_rw > off):
    forward = off
buckets = [1934,1958]
```

Из этого сразу следует, что запрос может быть **гарантированно** выполнен с
опцией `forward = off`.

```sql
INSERT INTO t VALUES ($1, $2), ($3, $4) OPTION (FORWARD = OFF) \bind 1 2 3 4 \g
```

Это позволяет убедиться, что:
1. в драйвере корректно реализован алгоритм вычисления `bucket_id`;
2. драйвер корректно выбрал узел для записи порции данных;

#### `SELECT` запросы

Рассмотрим запрос:
```sql
SELECT * FROM t WHERE a = $1 or a = $2 \bind 5 2 \g
```

Его `explain`:
```sql
forward analysis (on > ro_to_rw > off):
    forward = ro_to_rw
buckets = [219, 1410]
```

Проанализировав полученную информацию, пользователь сможет прийти к выводу, что
запрос будет выполним с опцией `ro_to_rw`. Таким образом, в конец запроса можно
дописать опцию `forward`:

```sql
SELECT * FROM t WHERE a = $1 or a = $2 OPTION (FORWARD = OFF) \bind 5 2 \g
```

Аналогично опция применима для запросов `UPDATE` и `DELETE`.

#### Указание опции connection string

Помимо дописывания опции в SQL запрос можно также модифицировать connection
string.

```
"postgres://postgres:T0psecret@127.0.0.1:4327?options=forward%3Doff"
```

В результате опция будет применяться к каждому SQL запросу. Это позволит **не**
дописывать значение опции `forward` в конец каждого SQL запроса.

## Decision Outcome

TBD
<!-- Chosen option: "title of option 1", because justification. e.g., only
option, which meets k.o. criterion decision driver | which resolves … | … |
comes out best (see below). -->

### Consequences

Проверка выполнимости опции `forward` не должна сказаться на производительности.
<!--
* Good, because positive consequence, e.g., improvement of one or more desired
  qualities, …
* Bad, because negative consequence, e.g., compromising one or more desired
  qualities, …
* … -->

### Deliverables

TBD
<!-- Describe what the deliverables are and how the implementation of/compliance
with the ADR can/will be confirmed. -->
